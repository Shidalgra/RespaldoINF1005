// 1. Declaración (La receta)
function saludarUsuario(nombre) {
    return `Hola ${nombre}, bienvenido al sistema de ingeniería.`;
}

// 2. Llamado (Cocinar la receta)
let mensaje = saludarUsuario("Sergio"); 
console.log(mensaje);
